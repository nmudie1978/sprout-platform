/**
 * Career Twin — "AI Future Self".
 *
 * Pure, testable building blocks (types, modes, persona, prompt) are safe to
 * import anywhere. Server-only resolution helpers live in ./resolve and must
 * be imported directly from there (they touch Prisma).
 */
export * from "./types";
export * from "./modes";
export * from "./persona";
export * from "./prompt";
