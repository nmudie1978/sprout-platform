"use client";

/**
 * DEV PAGE: preview of the AI & Future of Work section — specifically the
 * redesigned "AI models & certifications" centered panel. Visit /dev/ai-models.
 */

import { AiFutureOfWorkSection } from "@/components/insights/ai-future-of-work-section";

export default function AiModelsDevPage() {
  return (
    <div className="mx-auto max-w-5xl px-4 py-6">
      <AiFutureOfWorkSection />
    </div>
  );
}
